# Prefer the repo-root .env. This file is an alternate location.
# Copy values from ../.env.example — never commit real keys.

XAI_API_KEY=
LLM_PROVIDER=xai
LLM_MODEL=grok-4.5
# XAI_BASE_URL=https://api.x.ai/v1
