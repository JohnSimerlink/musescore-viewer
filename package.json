{
  "name": "musescore-viewer",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend MuseScore viewer with seeded scores, playhead, and audio",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "seed": "bash scripts/prepare-seed.sh"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
