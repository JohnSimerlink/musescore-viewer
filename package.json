{
  "name": "musescore-viewer",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Copland MuseScore viewer with NL command bar and seeded scores",
  "scripts": {
    "start": "node server.mjs",
    "start:prod": "bash scripts/start-prod.sh",
    "dev": "node --watch server.mjs",
    "agent": "cd agent && (test -d .venv || python3 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/python -m copland_agent",
    "agent:dev": "cd agent && (test -d .venv || python3 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && COPLAND_AGENT_RELOAD=1 .venv/bin/python -m copland_agent",
    "seed": "bash scripts/prepare-seed.sh",
    "test": "cd agent && (test -d .venv || python3 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/pytest -q",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "engines": {
    "node": ">=18"
  }
}
